# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-08-13

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->
- User wants `ROADMAP.md` to be the single source of truth for feature status and future direction.
- User prefers `README.md` to stay focused on basic project information and setup, not epic-by-epic status.
- User wants both documentation domains reconciled together: product/engineering docs and OpenWolf process docs.
- [2026-08-14] User prefers **local-first validation**: one deterministic command (`npm run validate`) that mirrors CI exactly, instead of preview/staging environments. "Everything should be validated through local workflow parity with CI."
- [2026-08-14] User expects explicit, intentional env handling — no silent defaults that could point local runs at production.
- User expects the OpenWolf protocol run *first* (STATUS → anatomy → cerebrum → buglog) before any edit, and housekeeping (STATUS/anatomy/memory/buglog) after.
- [2026-08-17] User rejected the warm-dark-espresso + serif-headline redesign from the first reframe pass: disliked the palette and said the Fraunces display serif "looks like a newspaper" for a home-finding app. Corrected direction: reference **Airbnb and Funda.nl** specifically — light theme (white/soft-neutral, not dark), a single friendly rounded-geometric sans across all text roles (landed on Plus Jakarta Sans, not a serif+sans editorial pairing), and explicitly asked to **restore glassmorphism** (frosted-glass cards), reversing the anti-generic "remove glassmorphism" call from the first pass. Takeaway: for consumer-facing home/listing-search products, check Airbnb/Funda-style references before committing to an editorial/warm-earthy direction — the target feel is "friendly modern marketplace," not "print/newspaper" or "moody dark SaaS." Glassmorphism itself isn't inherently an AI-tell here; it's the combination with dark theme + gradients + emoji that was the actual problem the first pass fixed.

## Key Learnings

- [2026-08-14] **Validation contract:** `npm run validate` = typecheck → lint → test → build:local. CI (`frontend-ci.yml`) runs the same four checks as separate steps. Never add a CI check that has no local equivalent.
- [2026-08-14] `next build` inlines `NEXT_PUBLIC_API_URL` at build time. `lib/api.ts` therefore throws when it is missing in a production build (fail fast) and falls back to `http://localhost:4000/api/listings` only in dev. `app/admin/page.tsx` imports `API_BASE_URL` from `lib/api.ts` — do not re-declare it.
- [2026-08-14] Node version is pinned in `.nvmrc` (24) and consumed by CI via `node-version-file`; `engines` allows >=20.
- [2026-08-15] **Test layout:** suites live in `tests/` (not `__tests__/`, not co-located), config is `vitest.config.mts`, shared data in `tests/fixtures/`. `globals: false` — import `describe/it/expect/vi` explicitly from `vitest`; adding a `types` array to `tsconfig.json` would have restricted ambient types, so jest-dom matcher types come from importing `@testing-library/jest-dom/vitest` in `tests/setup.ts`. Playwright (`tests/e2e/`, `npm run test:e2e`) is excluded from the vitest run and is not part of `validate`.
- [2026-08-15] Rendering any component that pulls in `next/link` (Navbar, Footer, and therefore the landing) needs an `IntersectionObserver` stub in jsdom — Link uses it for prefetch. It is stubbed globally in `tests/setup.ts`.
- [2026-08-15] `ListingCard` wraps `listing.descripcion` in typographic quotes (`&ldquo;…&rdquo;`) inside a single `<p>`, so `getByText(description)` fails on an exact match. Use `{ exact: false }`.
- [2026-08-15] `tsconfig.json` includes `**/*.ts(x)`, so `tests/` is type-checked by both `npm run typecheck` and `next build`. A type error in a test breaks the build, not just the test step.
- [2026-08-15] `lib/api.ts`'s mock/offline fallback (used whenever `fetch` throws, which includes `next dev` without `dev:api` running) must independently enforce the same public-feed contract the real API is expected to apply: `estado === 'activo'` only, `creadoEn` descending, and PIN validation on resolve. It's easy to add a feature to only one of the two code paths (API success vs. catch block) since they look similar; check both whenever touching `fetchListings`/`reportListing`/`resolveListing`.
- [2026-08-15] Modals driven by an `isOpen` boolean prop (PublishModal, ShareModal, PrivacyPolicyModal pattern) are rendered unconditionally by the parent and just `return null` while closed — the component instance never unmounts, so `useState(propValue)` only captures the prop once. Any prop meant to reset on every open (e.g. `defaultTipo`) needs a `useEffect` keyed on `isOpen` (see PublishModal), not a `useState` initializer.
- [2026-08-15] No `openwolf` CLI binary in PATH in this environment (`openwolf scan` / `openwolf designqc` fail with "not found"). When the SessionStart hook flags anatomy.md as possibly stale, verify by reading the actual target files directly instead of relying on scan output.
- [2026-08-16] `components/PrivacyPolicyModal.tsx` and `app/terminos-y-privacidad/page.tsx` are two independent copies of the same legal content (modal for quick consent, full page for `/terminos-y-privacidad`) — they must be edited together or they drift out of sync. Before trusting either one's claims about what data is collected, grep the actual form (`components/PublishModal.tsx`, `lib/types.ts` `CreateListingInput`) rather than the policy prose itself, since policy text is hand-written and not derived from the schema.
- [2026-08-16] The documented data-request channel (`docs/DATA_POLICY.md`) is a placeholder email `pendiente-definir@alojamientosolidario.co` with a real SLA (24h ack / 5 business days resolution) attached. When surfacing it in user-facing copy, show it honestly labeled as "en proceso de habilitación" rather than hiding the TBD status or fabricating a different-looking real address — and always lead with the self-service PIN-based resolve/retire flow, which works today with no backend dependency.
- [2026-08-16] `app/admin/page.tsx` calls `POST ${API_BASE_URL}/admin` (list_all + per-status actions, `x-admin-key` header) but `openapi.yaml` only documents `/listings`, `/listings/report`, `/listings/resolve`, `/listings/upload-url` — `/admin` has never been in the contract. This is pre-existing, not something introduced recently. Don't silently "fix" this by editing openapi.yaml on an unrelated task; it needs an explicit decision (flag it to the user or wait to be asked) since documenting it is effectively defining the contract, not just describing it.
- [2026-08-16] Real admin authentication is explicitly deferred to "Fase 2" in `ROADMAP.md` section 10 ("Panel de administración con autenticación real... si el volumen de reportes lo justifica"). The two hardcoded demo keys (`colombia2026admin`, `admin`) in `app/admin/page.tsx` are intentional for the MVP, not a bug — don't "harden" them unless asked.
- [2026-08-16] `lib/api.ts`'s `MOCK_LISTINGS` module array is the single shared offline data store for the whole app within a browser tab session (public feed, PublishModal's created listings, and now the admin panel all read/mutate the same array). This is a feature, not a coincidence: admin actions taken offline via `setAdminListingStatus` actually change what `fetchListings` shows on the public feed in the same tab. Keep new offline-fallback code pointed at this same array rather than inventing a parallel one.
- [2026-08-17] `tailwind.config.ts` never defined an `xs` breakpoint, so every `hidden xs:inline` (Navbar) silently behaved as permanently `hidden` — Tailwind doesn't error on an undefined screen prefix, it just never generates the rule. This combined with untruncated flex children (long logo title, full-text nav/publish buttons) to cause real horizontal viewport overflow on mobile (reported via a 402px-wide screenshot). Fix pattern: components meant to shrink/hide content per-breakpoint must use only breakpoints actually defined in `tailwind.config.ts` (`sm md lg xl 2xl` — Tailwind defaults, no custom `screns` block here), and flex children that must shrink need `min-w-0`/`truncate`, not just a hidden label at a nonexistent breakpoint.
- [2026-08-17] **City is a route, not a filter.** `lib/cities.ts` is the single source of truth (name + slug) for the 7 supported cities — never hardcode a city list elsewhere again (PublishModal and SearchFilters both used to). Each city lives at `/<slug>/` (`app/[ciudad]/page.tsx`, `generateStaticParams()` over all 7 — mandatory because `next.config.mjs` has `output: 'export'`, so every path must be knowable at build time; an unrecognized slug just 404s since no static file exists for it, `notFound()` inside the page is a secondary guard, not the real gatekeeper). `app/page.tsx` (`/`) is a client component that reads `localStorage['preferredCitySlug']` and `router.replace()`s to it (falling back to `DEFAULT_CITY`) — this has to be a *client-side* redirect, not `redirect()`/`next.config.js` redirects, because static export has no server to issue a real one. `components/CitySwitcher.tsx` is the one control that changes city (dropdown, `chip` variant next to the desktop title, `title` variant as the mobile navbar's primary identity) — it pushes to `/${slug}/` and persists the pick to that same localStorage key. Any component that needs "the current city" gets it as a prop from `components/CityFeedPage.tsx` (which `app/[ciudad]/page.tsx` renders) — there's no global city context/store beyond the URL + that one localStorage key. [[city-switcher-tests]]
- [2026-08-17] Any component using `useRouter()` from `next/navigation` (CitySwitcher, the `/` redirect) throws in tests without an app-router context — `tests/setup.ts` globally mocks `next/navigation` (`useRouter`/`usePathname`/`useSearchParams`/`notFound`) so any tree can render. [[city-switcher-tests]]
- [2026-08-17] `text-emerald-400` (and similarly light shades of any accent color) reads fine on the *dark* colored chips (`bg-solidarity-950`, `bg-rose-950`, `bg-emerald-900/NN`, `bg-black/NN` overlays) but is under-contrast on the light theme's actual page/card surfaces — check what's directly behind the text before picking a shade, don't assume one emerald shade works everywhere post-light-theme-flip. Text/icons on the plain light surface want a dark shade (`emerald-700`); text/icons on an intentionally dark badge/chip want a light shade (`emerald-300`/`-400`).
- [2026-08-17] Modal overlays needing both (a) vertical centering when content is short and (b) a scrollable-to-the-top overlay when content is taller than the viewport (so the close button stays reachable) should use `grid place-items-center` on the backdrop, not `flex items-center justify-center` — Flexbox's centering-plus-overflow interaction can make the start of an overflowing centered child unreachable by scroll (see the [[modal-overlay-pattern]] note above), CSS Grid centering doesn't have that bug. All 4 overlay modals (PublishModal, ShareModal, PrivacyPolicyModal, ListingCard resolve modal) use this now.
- [2026-08-17] The `mask-image` approach for HeroButtons' decorative background mural (see below) didn't render visibly for the user even after the luminance-masking fix, so it was abandoned per their request in favor of a plain `<img>` + CSS `filter` — simpler and more reliably supported. `public/solidaridad.svg`'s fill was reverted back to `#000000` (the mask-specific white-fill workaround no longer applies) and HeroButtons.tsx now renders it as an absolutely-positioned `<img src="/solidaridad.svg">` with `filter: invert(29%) sepia(96%) saturate(748%) hue-rotate(93deg) brightness(94%) contrast(92%)` (the standard invert+sepia+hue-rotate recipe for tinting a black SVG/PNG to an approximate target hue via CSS alone, no asset editing) plus `opacity-20`. If this still doesn't render or the tint needs adjusting, tune the filter values or opacity directly — this is a real `<img>` now, no masking-mode ambiguity to debug.
- [2026-08-17] All 4 overlay modals (PublishModal, ShareModal, PrivacyPolicyModal, ListingCard resolve-PIN modal) share one pattern: `fixed inset-0 ... flex items-center justify-center ... overflow-y-auto` on the backdrop, tall card centered inside. When the card is taller than the viewport this can leave its top (including the close `X`) unreachable by scroll. Fixed by using `items-start sm:items-center` + `py-8` on the backdrop instead of `my-8` on the card. Any new modal should copy this pattern (plus the click-outside via `onClick` checking `e.target === e.currentTarget`, and an Escape-key `useEffect` placed *before* any early `return null` so hook order stays stable), not the old `items-center`-only version.
- [2026-08-17] **Correction to the entry below**: the recurring `next build` `PageNotFoundError`/`ENOENT` this session was NOT stale `.next/` trace data — it's a live `next dev` server (the user's own preview session, PID visible via `ps aux | grep next`) sharing the same project's `.next/` directory. Running `rm -rf .next && next build` while that dev server is running races it: dev keeps writing into `.next` while build reads/expects its own manifests, producing exactly this ENOENT. **Do not run `next build` (let alone `rm -rf .next` first) while a `next dev` process is running against the same repo** — check `ps aux | grep "next dev"` first. Prefer `npm run typecheck && npm run lint && npm run test` to verify a change when a dev server is live; those don't touch `.next` and the user already gets visual feedback from their own dev server. Only run a full `build`/`build:local` when no dev server is running, or after confirming with the user it's safe to interrupt theirs.
- [2026-08-17] `next build` can throw a spurious `PageNotFoundError`/`ENOENT` for a page that exists on disk when `.next/` has stale trace data from an interrupted previous build (this diagnosis turned out to be wrong for the recurring case this session — see the entry above for the real cause found later. Left here since a genuinely stale `.next/` from a truly interrupted build is still a real, distinct possibility when no dev server is involved).
- [2026-08-17] `npx openwolf@2.0.1 designqc` doesn't exist as a subcommand (only `init/status/scan/dashboard/report/daemon/cron/update/restore/bug/help`) — the `.wolf/OPENWOLF.md` Design QC workflow describes a command this installed CLI version doesn't ship. Verify UI changes by reading the component source/build output instead of relying on `designqc` screenshots until the CLI is updated.
- [2026-08-17] **Design system overhaul, v1** (reframe fix, full app, later superseded — see v2 below): warm espresso-dark + terracotta/sage-teal palette, Fraunces serif headings. Rejected by user.
- [2026-08-17] **Design system overhaul, v2** (Airbnb/Funda-referenced correction, current state): `tailwind.config.ts` inverts the `slate` scale so the SAME class names (`bg-slate-950`, `text-slate-100`, `border-slate-700`, etc.) that every component already uses now resolve to a **light** theme — `slate-950` ≈ soft off-white `#f7f7fa` (page/card bg), `slate-50` ≈ near-black (primary text) — the whole app flipped from dark to light with zero per-component background/border edits. `rose` (coral, Airbnb-rausch direction) and `emerald` (fresh green) were repunched brighter/more saturated; `solidarity`/`emergency` keys mirror `emerald`/`rose`. Font: single `Plus Jakarta Sans` via `--font-sans` (no more serif; `--font-display` styling in `globals.css` falls back to the same sans) — do not reintroduce a serif for headings, it reads as "newspaper," not "home-finding app," per direct user feedback. Glassmorphism was **restored on user request** (`.glass-card`/`.glass-nav` in `globals.css` now `rgba(255,255,255,0.72/0.8)` + `backdrop-filter: blur(16px)`, not the flat opaque surfaces from v1) — light frosted glass is fine, it was never the actual problem. Because `bg-slate-950`/`text-white` were used as *dark-theme conventions* (dark page bg, white text-on-dark) throughout every component, the light-theme flip required manually auditing every literal `text-white` occurrence: kept where text sits on a genuinely dark/colored surface (solid CTA buttons, the rose-950 EmergencyBanner, badge chips on rose-950/emerald-950 cards, photo-overlay carousel controls now pinned to explicit `bg-black/60`), changed to `text-slate-100` everywhere it was previously "white text on the dark page/card" and would otherwise go invisible on the new light background. **When adding any new UI**: reuse `bg-slate-*`/`text-slate-*`/`bg-rose-*`/`bg-emerald-*`/`.glass-card`/`font-sans` — never hardcode `text-white` unless the element sits on an explicitly dark/saturated-color surface, and never hardcode `bg-slate-950/NN` for a scrim/overlay (it's now light) — use `bg-black/NN` for anything that must stay a dark translucent overlay (modal backdrops, photo-carousel controls) regardless of theme.
- **Project:** website-proyecto-colombia
- **Description:** React / Next.js 14 Web Application for **Alojamiento Solidario Colombia** (`website-proyecto-colombia`). Designed for survivors, volunteers, and citizens following the August 10, 2026 earthquake in Co

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->
- [2026-08-13] No usar `openwolf scan --check` directo en CI para `anatomy.md` porque siempre deriva por `Last scanned`. Normalizar/ignorar ese campo y alertar solo cuando haya drift estructural real.
- [2026-08-13] Evitar `concurrency.group` global para CI+deploy de Pages; usar grupo por ref para CI y grupo dedicado para deploy a `main` para evitar cancelaciones cruzadas. (Histórico: el deploy de Pages se eliminó el 2026-08-14; ver Decision Log.)

- [2026-08-14] No declarar scripts de validación sin su configuración: `next lint` sin `.eslintrc*` abre un wizard interactivo y cuelga runners no interactivos; `vitest run` sin suites sale con código 1. Verificar que cada script del contrato corre limpio en no-TTY antes de meterlo en `validate`/CI.
- [2026-08-15] No nombrar la config de Vitest `vitest.config.ts` en este repo: el `package.json` no declara `"type": "module"`, así que Vite la carga como CJS y advierte por sintaxis ESM en cada corrida. Usar `.mts` y `fileURLToPath(new URL('./', import.meta.url))` en vez de `__dirname` (que también dispara warning bajo `configLoader: 'native'`).
- [2026-08-15] `npm ci` verde en macOS **no** garantiza `npm ci` verde en el runner linux. Cuando se agrega una devDep que arrastra binarios nativos o fallbacks wasm (vite 8/rolldown, `@unrs/resolver`, `@emnapi/*`), npm actualizando el lockfile de forma incremental puede omitir entradas top-level que linux sí necesita → `EUSAGE ... Missing: X from lock file`. Regenerar el lockfile limpio (`rm -rf node_modules package-lock.json && npm install`) y, si hay dudas, verificar con `docker run --rm -v <dir>:/app -w /app node:24 npm ci` sobre una copia de `package.json` + `package-lock.json` (nunca sobre el repo montado: `npm ci` borraría el `node_modules` local).
- [2026-08-15] No dar por bueno un gate de tests solo porque las suites pasan: verificar además que un test en rojo hace fallar la cadena (canario temporal, borrar después). Un `include` mal escrito deja el paso verde sin correr nada.
- [2026-08-14] Antes de dar por buena una regeneración de `anatomy.md`, revisar el conteo de archivos: artefactos locales (`out/`, `*.tsbuildinfo`, `settings.local.json`) no existen en CI y provocan drift falso. Excluirlos en `.wolf/config.json`.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->
- [2026-08-14] **Sin ambiente de preview/staging del frontend.** La garantía de calidad se basa en paridad local ↔ CI: `npm run validate` corre exactamente los checks que CI exige. Alternativa descartada: ramas/environments de preview (costo de mantenimiento durante crisis activa, y nada que desplegar ahí porque la entrega productiva la opera infra). `US-8.2` queda `⏸️ aplazado`, no eliminado.
- [2026-08-14] **`NEXT_PUBLIC_API_URL` estricta en build, permisiva en dev.** Un default silencioso a producción en `lib/api.ts` significaba que `npm run dev` podía escribir datos reales; y un artefacto construido sin la variable apuntaría a un endpoint adivinado. Se movió ese default de producción al workflow de CI (visible, con `::warning`), y el código falla rápido en build.
- [2026-08-14] GitHub Pages approach removed entirely (workflow `deploy-pages` job + Pages artifact, ROADMAP references). Supersedes the 2026-08-13 dual-path decision. Entrega única: este repo hace build/type-check y expone el artefacto estático `out/`; el despliegue productivo (S3/CloudFront) lo hace `infra-proyecto-colombia`. `output: 'export'` en `next.config.mjs` se mantiene porque produce ese artefacto.
- [2026-08-13] ~~Delivery split: this repo publishes a permanent public mock on GitHub Pages~~ — superseded 2026-08-14 (GitHub Pages removed). Production deployment path (S3/CloudFront and infra automation) is owned by `infra-proyecto-colombia`.
- [2026-08-13] Documentation governance formalized: when docs disagree, `ROADMAP.md` prevails until reconciliation.
